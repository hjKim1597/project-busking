<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.busking.main.model.MainMapper">

	<!-- 공지사항 최신글 4개 -->
	<select id="getCustomerList"
		resultType="com.busking.board.model.BoardCustomerDTO">
		SELECT NOTICE_TITLE, NOTICE_REGDATE FROM NOTICE WHERE 4 >= ROWNUM 
		ORDER BY NOTICE_REGDATE DESC
	</select>

	<!-- 소식게시판 최신글 4개 -->
	<select id="getNewsList"
		resultType="com.busking.board.model.BoardNewsDTO">
		SELECT NEWS_TITLE, NEWS_REGDATE FROM BOARD_NEWS WHERE 4 >= ROWNUM 
		ORDER BY NEWS_REGDATE DESC
	</select>

	<!-- FAQ 최신글 4개 -->
	<select id="getFAQList"
		resultType="com.busking.customercenter.model.FAQDTO">
		SELECT FAQ_TITLE, FAQ_REGDATE FROM FAQ WHERE 4 >= ROWNUM 
		ORDER BY FAQ_REGDATE DESC
	</select>


</mapper>